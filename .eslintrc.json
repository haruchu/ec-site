{
  "extends": ["next", "prettier", "plugin:storybook/recommended"],
  "rules": {
    // import の順番をルール化
    "import/order": [
      "error",
      {
        "alphabetize": {
          "order": "asc"
        }
      }
    ]
  }
}
